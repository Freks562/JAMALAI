name: Institutional Heartbeat

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  heartbeat:
    runs-on: ubuntu-latest
    steps:
      - name: Call JamalAI Heartbeat Gateway
        run: |
          curl -X POST \
            "https://qvtujdcnzmmdcyxctggb.supabase.co/functions/v1/jamalai-gateway/heartbeat" \
            -H "Content-Type: application/json" \
            -H "X-Heartbeat-Source: github"

