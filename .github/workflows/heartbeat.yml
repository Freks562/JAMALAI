name: JamalAI Heartbeat

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  heartbeat:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase heartbeat
        run: |
          curl -X POST \
          "https://qvtujdcnzmmdcyxctggb.supabase.co/functions/v1/github-heartbeat"
